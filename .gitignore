
# Internal AI workflow artifacts (contain real example data) — do not publish
docs/superpowers/

# OS junk
.DS_Store

# Raw source art (kept locally; site uses the vectorized SVGs in images/)
/Ganesha.png
/OM-Symbol.png

# Local design preview (not part of the published site)
/invitation-preview.html

# Floral/image source libraries (only the composed images/ assets are published)
/FloralLineSVG/
/Flower vectors/
/Images for Gallery/
/fullsite-preview.html
